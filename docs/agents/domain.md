# Domain docs

Use a single-context layout: root CONTEXT.md and docs/adr/.

Before exploring, read CONTEXT.md and relevant ADRs.
If these files are absent, proceed silently. Domain-modeling
creates them when terminology or decisions are resolved.

Use the glossary’s domain terminology. Identify terminology
gaps for domain-modeling and explicitly surface ADR conflicts.
